name: gds

on:
  push:
  workflow_dispatch:

jobs:
  gds:
    runs-on: ubuntu-24.04

    steps:
      - name: checkout repo
        uses: actions/checkout@v4

      - name: Build GDS
        uses: TinyTapeout/tt-gds-action@main
        with:
          pdk: sky130A
